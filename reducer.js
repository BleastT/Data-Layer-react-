export const initialState = {
  //The data is stored as a dictionnary
};

const reducer = (state, action) => {
  switch (action.type) {
    //use cases to trigger a change in the data layer
    default:
      return state;
  }
};

export default reducer;
